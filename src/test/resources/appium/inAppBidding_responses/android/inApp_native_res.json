{
  "id": "$VARIABLE",
  "seatbid": [
    {
      "bid": [
        {
          "id": "$VARIABLE",
          "impid": "$VARIABLE",
          "price": "$VARIABLE",
          "adm": "$VARIABLE",
          "crid": "$VARIABLE",
          "w": "$VARIABLE",
          "h": "$VARIABLE",
          "ext": {
            "prebid": {
              "cache": {
                "bids": {
                  "url": "$VARIABLE",
                  "cacheId": "$VARIABLE"
                }
              },
              "targeting": {
                "hb_bidder": "$VARIABLE",
                "hb_bidder_openx": "$VARIABLE",
                "hb_cache_host": "$VARIABLE",
                "hb_cache_host_openx": "$VARIABLE",
                "hb_cache_id": "$VARIABLE",
                "hb_cache_id_openx": "$VARIABLE",
                "hb_cache_path": "$VARIABLE",
                "hb_cache_path_openx": "$VARIABLE",
                "hb_env": "$VARIABLE",
                "hb_env_openx": "$VARIABLE",
                "hb_pb": "$VARIABLE",
                "hb_pb_openx": "$VARIABLE",
                "hb_size": "$VARIABLE",
                "hb_size_openx": "$VARIABLE"
              },
              "type": "native"
            },
            "origbidcpm":"$VARIABLE",
            "origbidcur":"$VARIABLE"
          }
        }
      ],
      "seat": "$VARIABLE",
      "group": "$VARIABLE"
    }
  ],
  "cur": "$VARIABLE",
  "ext": {
    "responsetimemillis": {
      "cache":"$VARIABLE",
      "openx":"$VARIABLE"
    },
    "tmaxrequest":"$VARIABLE",
    "prebid":{
      "auctiontimestamp":"$VARIABLE"
    }
  }
}